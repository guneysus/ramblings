source "https://rubygems.org"

# Use the GitHub Pages gem to mimic the exact build environment
gem "github-pages", group: :jekyll_plugins

# If you are on Ruby 3.0 or later, you may need to add the 'webrick' gem
# which provides the web server for local serving.
gem "webrick", "~> 1.7"